/* app.jsx — Standalone entry point for the production website.
   Renders the chosen landing direction (B · Big-Type Editorial) at full bleed. */

ReactDOM.createRoot(document.getElementById('root')).render(<LandingB />);
